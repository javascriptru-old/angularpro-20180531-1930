import { ColoryDirective } from './colory.directive';

describe('ColoryDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoryDirective();
    expect(directive).toBeTruthy();
  });
});
